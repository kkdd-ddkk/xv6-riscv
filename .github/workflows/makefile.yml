name: Makefile CI

on:
  push:
    branches: [ "riscv" ]
  # pull_request:
  #   branches: [ "riscv" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: gregdavill/setup-riscv-gnu-toolchain@v2.0
    - uses: actions/checkout@v4

    - name: Make
      run: make
    
    
    # - name: configure
      # run: ./configure

